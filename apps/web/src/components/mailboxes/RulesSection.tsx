// Composition facade (Otter parity): Meow has no processing rules.
// Kept as a facade so future rule UI can split into ruleLabels/formatCondition/RuleList/RuleEditor.
export default function RulesSection() {
  return null;
}
