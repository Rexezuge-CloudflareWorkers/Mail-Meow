// Meow has no outbound integrations. Stub preserves Otter's
// useMailboxes -> useApplications + useIntegrations delegation shape.
export function useIntegrations(_options?: unknown) {
  return {};
}
